package site.soulware.cocina360.security.interfaces.rest.presence;

/**
 * Which kind of device a presence entry describes — the gateway itself, or one of the
 * IoT sensors it relays for.
 */
public enum DeviceKind {
    EDGE,
    IOT
}
